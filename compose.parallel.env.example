# 项目 1 与项目 3 并行时：复制为 .env 或在 shell 里 export 后再 compose up
# 项目 1（默认端口保留）
# FRONTEND_HOST_PORT=80
# BACKEND_HOST_PORT=8000
# POSTGRES_HOST_PORT=5432
# PROMETHEUS_HOST_PORT=9090
# GRAFANA_HOST_PORT=3000

# 项目 3 并行示例（避免与项目 1 / Cursor New-API 打架）
FRONTEND_HOST_PORT=8081
BACKEND_HOST_PORT=8200
POSTGRES_HOST_PORT=15433
REDIS_HOST_PORT=6380
CHROMA_HOST_PORT=8002
PROMETHEUS_HOST_PORT=9191
GRAFANA_HOST_PORT=3101
PROJECT1_API_BASE=http://localhost:8100
